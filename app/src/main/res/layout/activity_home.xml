<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_home"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.emma.stretchlauncher.MainActivity"
    android:background="@color/colorPrimaryDark">

    <Button
        android:text="New Swim"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/new_swim"
        android:textSize="30sp"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:background="@color/colorAccent"
        android:minWidth="250dip"
        android:minHeight="51dp"
        android:textColor="?android:attr/textColorPrimaryInverse"
        android:textAllCaps="false"
        tools:onClick="title_activity_create_new_swim"
        android:onClick="new_swim_pressed" />

    <Button
        android:text="@string/previous_swim"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/previous_Swim"
        android:textSize="30sp"
        android:layout_marginTop="31dp"
        android:layout_below="@+id/new_swim"
        android:layout_centerHorizontal="true"
        android:textColor="?android:attr/textColorPrimaryInverse"
        android:background="@color/colorAccent"
        android:minWidth="250dip"
        android:minHeight="49dp"
        android:textAllCaps="false" />

    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/aislogo"
        android:layout_marginBottom="34dp"
        android:id="@+id/imageView"
        android:layout_above="@+id/new_swim"
        android:layout_alignRight="@+id/new_swim"
        android:layout_alignEnd="@+id/new_swim" />

</RelativeLayout>
